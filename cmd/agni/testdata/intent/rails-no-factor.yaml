# Budgets with NO margin_factor. The margin rule has no default policy, so it is not compiled at all
# and item 19 reads needs-design-intent rather than passing against a number nobody declared.
name: Rail budgets (no margin policy declared)
rail_budgets:
  - {rail: 3V3, peak: 0.4}
  - {rail: 1V8, peak: 0.4}
