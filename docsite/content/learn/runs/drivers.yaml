# Chapter 5's spine. Two nets, opposite defects, three rules. FIGHT has two drivers and fails
# contention; FLOAT has none, PASSES contention correctly, and fails the chapter 4 rule instead.
# That pass is the point of the section: it answers one question honestly and no others.
fixture: cmd/agni/testdata/conformance
script: |
  agni check --verdicts --rule output-output-conflict --rule floating-input --rule single-pin-net directions.fires.edn
