# Out of scope — the deferred-work ledger

One line per item a PR deliberately deferred without a roadmap ticket. The contract:

- A PR that lists something under "Out of scope" adds it here in the same PR.
- An entry names the source PR (plain text, no backlink) and the natural pickup trigger.
- Remove the entry when the item lands or gets promoted to a roadmap ticket. Items that
  need evidence tracking, affect correctness, or have no adjacent future work get a
  ticket instead of a line here.
- **Sweep it periodically.** This file is only ever appended to in the course of normal work,
  so it drifts in two directions on its own: entries stay after the thing they asked for is
  built (a stale line reads as current, which is worse than a long list), and one piece of
  work gets written down many times as several PRs each defer their own slice of it. Both
  need a read-through to see; neither is visible one entry at a time. The 2026-08 sweep took
  108 entries to 57 by promoting eight clusters to tickets and deleting what had landed.
- **Nothing private.** This file is world-readable. A pickup trigger that cannot be stated
  without naming a customer, a private path, or a corpus location belongs in the private
  workspace, not here. Sanitize when writing the line, not later; deleting it afterwards does
  not remove it from history.

| Item | From | Pick up when |
|------|------|--------------|
| Details-panel summary names the sheet the design OPENED on and never updates as you navigate sheets, so it reads stale ("Hier root · faithful" while amp1 is shown). Pre-existing; the WS9-049 tab strip makes it visible by putting the active sheet name right beside it | WS9-049 PR1 | someone re-does the Details panel, or a user reports the mismatch; the fix is to push the summary per sheet rather than per design load |
| Symbol/multi-file resolution for an out-of-module reader — `Loader.symbolOpener`/`kicadSymOpener` stay unexported, so an externally-registered reader can open its own path but not get `--symbol-path` resolution or a hierarchy opener | WS12-003 PR | the first overlay reader needs multi-file resolution; expose an opener the Loader hands to the registered reader (mirror the built-in `ReadWithSymbols` seam) without leaking file paths into core (C1) |
| Surface `PASSIVE` in the spec direction vocabulary (dirString lumps it into "unspecified"; splitting changes unconnected-pin's skip set and the deferred unspecified-with-driver matrix row) | WS1-009 PR | the first rule that needs to distinguish a passive pin from an untyped one — re-audit unconnected-pin's guards in the same change |
| General diode-orientation (zener/TVS-safe) beyond led-polarity | WS1-009 PR | net-polarity facts exist (which way current is meant to flow), not just rail names |
| Export the spec vocabulary registries (RegisterFact/RegisterEntitySet/RegisterCollection mirroring RegisterSpecFunc: declared reads/primitives + namespace) | WS1-009 PR | WS10-005's parameterized param collections or the first embedder vocabulary. Registry core landed (WS3-006): vocabulary would hang off RuleSource with the source's namespace plugging into Available. Optimization is NOT this trigger: one name = one meaning, faster impls swap behind Model |
| NC-channel granularity for `unconnected-pin`: the gate is design-wide (any NC evidence anywhere enables the rule); a per-part-library gate may be righter | PR 93 | a design fires unconnected-pin with findings clustered on components whose own part types carry no NC evidence (a mixed-library design) — that failure mode is the evidence the design-wide gate lacks today |
| Schematic-reader placeholder refs (`R?` symbols merge by ref like REF** did on the PCB side) | WS1-024 PR | a corpus schematic with unannotated symbols shows the merge; same placeholderRef treatment, minding stub-net naming |
| `Validate` RPC serving `webapi.ValidateReport` to the viewer | PR 83 | WS9 wants a validation panel; the message shape is ready |
| `.edn` content sniffing, per-format validate knobs, unresolved-vs-empty cause split | PR 83 | ticketed as WS6-008 (listed here only until it lands) |
| Move `svg/` under `internal/` | PR 69 | next PR touching `svg/` |
| Rename `edif.ReadSchematic` to `ReadSchematicGeometry` (match the kicad naming) | PR 69 | next PR touching the edif reader surface |
| Tests for the remaining render-only islands (controlbar, expectationspanel) | PR 67 | next web PR; scaffolding exists; findingspanel landed in PR 84 |
| Corpus-scale parse benchmarks | PR 67 | a perf question actually arises; fixture benchmarks are the tripwire |
| `stats --format json` (canonical wire shape question; `diff --format json` landed with WS9-004) | PR 68 | someone needs machine-readable stats; decide the proto first |
| WebGL rendering for the visual diff (resolve sideSpecs locally over packed keys) | WS9-005 PR | WS9-007 overlay work, or when WebGL fidelity matches SVG |
| Diff deep-link URL (comparison state in the router) | WS9-005 PR | someone wants to share/restore a diff view; router owns the shape |
| Conformance fixtures for ipc2581 / xschem / geda source formats | PR 65 | those readers gain geometry-bearing fixtures worth asserting rules over |
| Netclass consistency (WS3-017's second half: one net, two netclasses) — **requalified, not a default-on rule**. WS1-050 established that multi-membership is legitimate in KiCad (it unions every match, then cascades per-class values by priority) and merely discouraged in Altium (the clearance matrix errors on it). So "two netclasses" is a house-style question, not a universal defect, and shipping it as a catalog rule would fire on correct KiCad projects | WS1-050 PR | a house or profile wants deterministic single-class membership — author it as a PROFILE-scoped or overlay rule, never a default-on catalog one. The engine half is done: `net.netclass` is 1:many, so the rule is now expressible as a self-join |
| Cross-library `extends` in external .kicad_sym resolution (same-library chains flatten today) | WS1-016 PR | first corpus/real library hit that derives across libraries; the resolver cache is where it lands |
| Register `.edf`/`.edif` as CLI extensions for the EDIF netlist reader (only `.edn`/`.eds` are today, so the corpus sweep aliases `.EDF`→`.edn`) | WS1-026 PR | someone runs the CLI directly over the `.EDF` corpus; one `readers/formats/registry.go` alias entry pointing at the existing edif reader |
| xschem power-input-not-driven (xschem `dir` has no power-pin type, so no power_in pins exist) | WS1-021 PR | xschem symbol libs that type supply pins, or a name-based power-pin heuristic; gEDA already reaches it via pintype=pwr |
| xschem title-block field polish: the schematic-title default text (xschem's "@schname"/global title, e.g. "Two stage amplifier SPICE playpen") is not resolved (only @author, @schname_ext=filename, and static text are); @time_last_modified/@path globals render blank; code_shown's `font=Monospace` is dropped | WS7-037 PR | someone wants pixel-faithful xschem title blocks; resolve the xschem title/global fields and carry a font hint on geom.Field. gEDA already shows the full title (it stores those as promoted attributes) |
| A slotted package's `PartType.Pins` lists only the drawn gate's pins (populated from the first-resolved slot), not the full physical package (7414 → the two gate pins, not all 14) | WS1-032 slotting PR | a consumer needs the complete package pin list per PartType — accumulate the union of every slot's remapped pins onto the library entry (the netlist is already correct; this is a library-completeness gap) |
| Fact-relation docs for the string-predicate built-ins (`contains`/`prefix`/`suffix`). They are eval built-ins, not EDB projectors, so they carry no `check/facts.go` back-link and are outside the per-relation require-all set. `reaches` (also a built-in) got a doc via the `predicateDocs` allowlist; the three string predicates are trivial and were deferred | WS14-005 (backfill PR) | someone wants the query cookbook to link a reference for `contains`/etc.; add a short `predicates` page (or three docs) and extend `predicateDocs` in `check/facts_docs_test.go` |
| WS3-072 PR3 (LOW PRIORITY — the gate is the intended PERMANENT answer, not pending work): a symmetric power-OUTPUT stamp that would let `power-input-not-driven` run on EDIF. NOT worth doing: the rule catches a rail drawn-but-never-sourced, which on a real EDIF export (already through layout + native ERC) is ~never genuine (all EVT firings were false — the source was present, just under-typed), and the one place it could catch something (schematic-stage EDIF) has the same under-typing that makes it unreliable. `design.types_power_out` gating it off is CORRECT permanent behavior, because EDIF genuinely cannot distinguish "undriven" from "source-under-typed". The WS3-072 value already landed in the two power-in-only rules (`decoupling-present`, `input-protection`) + the datasheet supply-pin rules. | WS3-072 PR2 | ONLY if undriven-rail detection on EDIF is genuinely needed. Even then, don't stamp POWER_OUT by name (`VOUT` is also a signal name); the cheaper path is device-class SOURCE recognition — a regulator/PMIC/switch output on the rail ⇒ driven — then drop the gate |
| WS3-072: the remaining per-rule `isPowerRailName`/`isGroundName`/`isFeedbackName` call sites (reach.go, pinrole.go, the rule Evals) still call the name lexicon; only the net.ground/feedback/IsPowerRail projectors read the stamped `net.role`. Mechanical, behavior-preserving (stamped = name-derived in PR1) | WS3-072 PR1 | migrate a call site to `netHasRole` when it is next touched; the FFIs (`rail_name`/`ground_name`/`feedback_name`) operate on a bare name literal with no net binding, so they keep the name lexicon by design |
| WS9-035 params panel reads via a bespoke `GetComponentParams` RPC (returns the structured `PartSpec`) rather than the datalog query surface (`component.mpn` ⋈ the WS10-010 `param(...)` relations). Considered the query route; kept the RPC because the panel's tree needs the full nested spec — conditions, provenance/citation, limit-kind, min/typ/max — and the flat `param(...)` relations do not carry all of that yet | WS9-035 PR 313 | WS3-082 enriches `param(...)` with min/limit_kind (and ideally conditions/provenance); then the panel can migrate to a datalog query over the existing QueryService and drop the RPC + adapter. Separately: point serve's provider at a real service-backed `param.ParamProvider` (the WS10-010 seam) instead of only `--params <dir>` |
| WS3-096: auto-derive `Rule.RequiresCapability` from a spec twin's `design.*` capability facts. Both rules the PR gates (`power-input-not-driven`, `unconnected-pin`) are Go-authored and declare the field by hand; a future spec-authored rule that guards on `design.types_power_out` / `design.nc_channel` could have the requirement derived from its Where clause the way `Reads`/`Primitives` are, so the declaration and the guard cannot drift | WS3-096 PR | a spec-only rule ships that gates on a capability fact; add capability derivation to `Spec.Rule()` alongside `DerivedReads`, and hold the Go rules to it in `TestSpecMetadata` |
| WS3-098 blocked-on-data residue: `needs-data` is DERIVED only for the datasheet-query case (a `param_symbol` binding whose symbol is seeded nowhere, WS3-097). A check waiting on a `--conventions` naming lexicon, and anything the overlay marks `owner: waiting-on-you`, still read not-automated | WS3-097/098 PR | Add the NEXT per-blocker derivation, do NOT import the overlay `owner` axis into the engine. The engine can only PROVE "blocked on data" from an engine-visible signal, and each concrete blocker has one: design-intent → `--intent-path` not loaded (`needs-design-intent`), datasheet value → `param_symbol` + model (`needs-data`). The conventions-lexicon case is STRUCTURALLY the intent case (a mechanism needing an external file), so it is derivable the same way — a `needs-config` off "was the `--conventions` source loaded", no `owner` needed. A `review.Item.Owner` was considered and rejected: `owner` (who unblocks it) is customer/overlay workflow metadata, and carrying it in the shareable engine crosses the open-core boundary Agni keeps against market/strategy material. The genuine residue after `needs-config` (a human asserting supplyability with zero design/binding/config evidence) SHOULD stay overlay metadata and read plain not-automated from the engine; the overlay checklist rollup carries the `owner` nuance. Tally care: needs-data/needs-config count as covered (WS10-014), so mis-deriving moves coverage numbers |
| WS3-103: no `CheckService` rpc returns a `CheckResults`, so `serve` cannot hand a client a CHECK document. The review half landed with the review resources (a stored run IS the document); this is now the check-only remainder | WS3-103a PR | a web client wants a downloadable report, or a CI job wants the document over HTTP; add the rpc plus its Connect adapter (the three-things rule), and move the design hash to whatever edge reads the bytes there — `internal/service` is filesystem-free by design, so hashing has to stay at the producing edge |
| WS3-103: `DesignRef` carries the source path and a content hash but not the design's own name or source format. Filling them would cost the writing edge a second parse (it is a thin service client and never loads a design), so they were left out rather than left empty | WS3-103a PR | a consumer needs to group documents by format, or WS3-104's differential harness wants the format to pick an oracle; get them from the run instead of a re-read — either the check response carries them or the document is written by a surface that already has the Model |
| WS3-103: the content hash covers the ENTRY file only, so a hierarchical design's sub-sheets and a project's sidecars are outside it. A matching hash means "the same entry file", not "the same design" | WS3-103a PR | a findings diff between revisions starts producing false "same revision" matches on a multi-file design; hash the resolved file set the loader actually opened, which needs the loader to report it |
| WS3-103: `review --results-out` is one design per document and refuses a rollup. The aggregate surfaces (`RenderAggregateMarkdown` / `RenderAggregateJSON`) have no document form | WS3-103a PR | a project rollup needs archiving; either write one document per design into a directory, or add an aggregate document that REFERENCES per-design ones rather than inlining them (a single doc carries one `DesignRef` on purpose) |
| WS3-104: the review runner cannot yet express "covered by an external result". An item whose real answer is "the vendor DRC checks this" still reads not-automated (understating it) or carries a human note. The import and the document exist; the review binding, the outcome, and the `RunParams` channel do not | WS3-104 PR | a real project checklist has items in exactly this state; add a `review.Binding.External{producer, rule}` plus a `covered-externally` Outcome that NAMES the producer, fed by imported documents on RunParams. It must not be spellable as a plain pass: a vendor report has no coverage axis, so "checked and clean by another tool" is a weaker claim than a native pass and has to read differently in the Tally |
| WS3-104: the differential harness compares one design at a time via `agni results --compare`. There is no corpus sweep that runs both tools over many boards and aggregates the split | WS3-104 PR | someone wants a regression gate rather than a spot check; add a sweep target where the corpus lives (the engine side is already there — the loop is shell), and decide whether an aggregate is a report or a document |
| WS3-104: the import joins by the names a vendor prints, never by uuid. KiCad's uuids identify board and schematic OBJECTS (this track, this pad) and our model is a netlist of components and nets, so there is nothing to join a track uuid to; they are carried as `Provenance.native_id` only | WS3-104 PR | the geometry tier grows an object-level index a uuid could resolve against (WS9 highlight already keys wires by uuid), at which point a track-level violation could locate precisely instead of joining to its net |
| WS3-104: only KiCad is a producer. The item-description parse table is KiCad-specific by nature (they are UI strings), so a second vendor means a second table, not a shared one | WS3-104 PR | a second tool's machine-readable report is available; keep each vendor's table in its own file with its own form-matrix test, and resist factoring them together — the shapes have nothing in common but the job |
| WS3-107: the services still hold a BUILT `*check.Catalog` rather than the sources that composed it, so `Overlay.Catalog` extends a catalog it cannot re-derive. `Catalog.With` makes the drop impossible at the one site that had it, but the shape still permits a future caller to rebuild and lose the base | WS3-107 PR | the ticket's preferred fix: thread a "catalog inputs" value (`[]check.RuleSource`) to every surface so a catalog is always composed from its inputs and this class of drop is structurally impossible. It is a signature change across ~20 call sites, mostly tests, which is why it was kept out of a P1 bug fix |
| WS3-108 audit: `Reach` never RECORDS a bus-like net, it skips it entirely (`reach.go` `if IsBusLike(m, o) { continue }`), so any guard looking for something that sits ON a wide net has the same blind spot the pull-up check had. Audited the five `Reach` consumers in `core/check/guards.go`: `TVSReachable`, `ZenerReachable` and `ICESDRated` look for a component on the SIGNAL side and are unaffected; `PowerPinReachable` and `UnprotectedPowerReach` look for a POWER_IN pin on a reachable net, and a regulator's VIN net can exceed maxWalkFan, so they are structurally exposed | WS3-108 PR | the mechanism is certain from the source; the practical impact is NOT measured, and measuring it needs the real boards. Gather that evidence first (count how often a power-in pin sits only on a >16-fan-out net across the corpus), then decide between letting the walk TERMINATE on a wide net without crossing it — which changes `Reach().Nets` for all five consumers plus `Between` and the public `reaches` relation — or giving these two guards their own narrow predicate as the pull-up check now has. Do not change the shared walk without that evidence |
| WS3-085: the EDIF reader builds part-type pins with a NAME and an EMPTY designator, while connections reference the port by name, so `Model.PinRole(ref, pinRef)` finds no declared pin and every EDIF pin reads unknown. Every pin-role rule is therefore silent on EDIF (`reverse-blocking-absent`'s diode orientation, and now the external-FET load-switch topology). The value side is fine: `loadswitch.silent.edn` proves the same design's `0R01` shunt normalizes to 0.01 ohms exactly as on KiCad, so the caveat this ticket carried (that EDIF would not pick up the value) is not the live blocker | WS3-085 PR | someone needs a pin-role rule on EDIF. Set `Pin.Designator` to the port name in the EDIF reader so connection PinRefs resolve, then re-run the whole conformance corpus: several rules currently silent on EDIF start evaluating, and each firing has to be judged before the change is accepted. `loadswitch.silent.edn` is the tripwire that will fail when this lands |
| WS3-085: a series GATE RESISTOR between the controller and the FET defeats the topology resolution, which requires a direct connection. Common in real designs | WS3-085 PR | a real board's switch is missed. Crossing passives via `Reach` would also cross a gate pull-down into the source net's neighbourhood and could bind the wrong controller, so the fix is a narrow gate-path walk (resistor only, one hop, far side must carry the controller's drive pin), not the generic reach |
| WS3-095: the rail-sizing rules are silent on a rail no seeded part supplies, and the review runner's needs-data gate that covers the unseeded case is DESIGN-WIDE (nothing on the board states an output current) rather than per-rail. A design where one regulator is seeded and the one feeding this rail is not still reads pass | WS3-095 PR | a project seeds its power tree partially and wants the gap named per rail; the runner would need a per-subject data verdict, which the outcome vocabulary does not have today (an outcome is per review ITEM). Decide that shape before widening the gate |
| WS3-095: `margin_factor` is ONE factor for the whole declaration, so a project cannot ask for more headroom on a processor core rail than on an LED rail. Kept flat because a per-rail factor is a second policy surface and no consumer has asked for it | WS3-095 PR | a real project needs per-rail headroom; add an optional per-budget `margin_factor` that falls back to the declaration-level one, and keep the no-default rule at both levels |
| WS3-092: `relation` has one value, `enable-gated`. A rail order enforced by a SEQUENCER PART owning both rails is the other structure a netlist could half-evidence (the part is there, its configuration is not), and it is not expressible today | WS3-092 PR | a design has a sequencer whose channel-to-rail mapping is readable (a seeded PartSpec, or pin roles); add a second relation rather than loosening this one, since the two ask different questions of the netlist |
| WS3-092: a gating link through TWO parts in series (power-good to comparator to delay element to enable) reads as absent, because the credit test is one part wide. Deliberate: widening it is how a design with no chain starts reading as sequenced | WS3-092 PR | a real board's chain is missed; prefer declaring the intermediate node as a stage handle over widening the radius, and only then consider a bounded multi-part walk |
| WS3-092: `intent.DocRules` stamps every intent doc page `severity: warning`, so the docsite catalog understates the three intent rules whose runtime severity is `error` (`rail-current-capacity` from WS3-095, `sequence-<slug>` here, `load-switch-trip-below-budget` from WS3-085). The runtime findings carry the right severity; only the generated reference page is wrong | WS3-092 PR | the doc-rule projection is touched for any reason; carry the per-kind severity in a map beside `docSummaries` rather than hardcoding one value |
| WS3-085: where two resolved load switches reach one rail the HIGHEST limit binds, so a rail genuinely gated by the smaller of two switches reads clean. Deliberate, and the same trade the supply side makes: the reach radius can pull in a switch gating a different branch, and every FAIL has to be a genuine defect, so ambiguous evidence takes the reading that does not fire | WS3-085 PR | the association can distinguish "gates this rail" from "is within reach of this rail" — which needs directionality the netlist does not carry today. Until then, prefer declaring the budget on the net immediately downstream of the intended switch |
| WS9-049 phase 2: a previewed design is not addressable. The browse URL space stays folders-only (`/designs/<mount>/<dir>/`), so a preview cannot be shared or restored by refresh — deliberate, because the URL that names a design is its work page, and pushing that from the browse page would make Back walk through previews | WS9-049 phase 2 PR | someone wants to link a colleague at "this design, in the browser". Add a `?preview=<path>` query the browse page restores, keeping the folder as the path so the two levels stay distinguishable |
| WS9-049 phase 2: the browse preview is the FIRST drawable sheet (or the board), never a multi-sheet flip. A hierarchical schematic previews only its root sheet, so a design whose interesting page is sheet 3 looks empty in the browser | WS9-049 phase 2 PR | multi-sheet designs get browsed often enough that the root sheet misleads; add a sheet pager to the preview header. Deliberately not a dock panel or a tab strip — those are work-page surfaces, and the browse page must not grow into a second viewer |
| WS9-049 phase 3: browse-initiated comparison (select two files on `/designs`, land on the diff) is NOT built. The ticket proposed it as the primary entry point; the work-page compare picker shipped instead, because a second entry point forces a URL that NAMES a comparison, and committing to a one-pair URL shape before deciding whether comparisons are singular or plural is the expensive half. The picker needs no URL at all (the diff is in-page state) | WS9-049 phase 3 PR | after the N-comparison question is settled. Doing it first would likely mean reshaping the URL a second time |
| WS9-049 phase 3: the viewer holds ONE comparison. `openDiffPanel` hardcodes panel ids `diff`/`changes`, `ViewerPage.html` has one hole each, and `main.ts` builds a single `DiffPresenter` — so comparing design A against three versions means three sequential comparisons, not three views. The picker was deliberately shaped to report "a design to compare against" rather than "side B", so the presenter/panel layer is the only thing N has to change | WS9-049 phase 3 PR | the user wants several comparisons live at once. Cheapest shape is ONE diff panel holding a SET with a comparison selector beside the existing sheet-pair selector; tiled per-comparison panels instead need dynamic panel ids and dynamically created holes, which cuts against the dock's adopt-a-server-rendered-hole model (see the "no lazy cloning" note in dock.ts) |
| Packaging PR: `--pdf2doc` and `--enable-native` are process-EXEC seams (the server shells out to a binary on its own PATH), which forces the choice between one giant image and a broken feature. Both want to be endpoint seams instead; `Dockerfile.nattools` is already this pattern done by hand over SSH | packaging PR | the `-eda` image or the docling sidecar is built. Convert both seams in one change rather than one at a time, since they share the shape, and decide it BEFORE a second image hardcodes the exec assumption |
| Packaging PR: the hosted/upload deployment is not built. It needs no second image and no Dockerfile change. `service.Loader` and `service.Workspace` are already defined over `(mount, path)` with every filesystem detail in `cmd/agni`'s `os*` adapters, so a session-scoped backend selected at startup is the whole shape. New surface is upload itself (multipart, not proto), session lifecycle with a TTL, and a quota | packaging PR | a hosted deployment is wanted. Note the datasheets workbench already WRITES back (SavePartSpec / SaveAnnotations), so on a shared host with no auth that is a write surface between anonymous users, not just a read-visibility problem; sessions scope it incidentally |
| GHCR publishing PR: the docs PIN a version (`ghcr.io/panyam/agni:v0.1.1`, `go install ...@v0.1.1`, the `agni version` sample output), and nothing detects that they are stale after the next release. Pinning is deliberate, since a report is only reproducible if you can name the build, so the fix is not to unpin | ghcr-publish follow-up PR | the version in the docs is noticed to be behind. Cheapest durable fix is a docsite test that greps the published version references and compares them against the newest git tag, in the shape `catalog-docs-check` already uses (regenerate, then fail on drift). Until then treat a release as a two-part job: tag, then bump the doc references |
| `DiffDesignsRequest.a_path` / `b_path` are artifact refs wearing a misleading `_path` name, the same C22 misnaming WS9-050 fixed on `RunReviewRequest`. They were left alone because the diff RPC has live web consumers (the compare picker), so the rename is a real wire break rather than the free one the review request was | WS9-050 PR | the diff request is reshaped for another reason, or a second consumer is about to be written against it. Rename both to `*_ref` and update `web/src/compare*.ts` in the same change; the C22 Verify's "no `*_path` on a config message" sweep stays un-automatable while these two exist |
| `agni serve` has no authentication, so every stored review run in the `--review-store` volume is visible to every client of the server, as are the datasheet workbench's write surfaces. Deliberate for now: the deployment assumption is one team on a trusted network | WS9-053 PR | the server is exposed beyond one team, or a second team shares a deployment. Auth is the prerequisite for per-user or per-project scoping of reviews; until it exists, do NOT add a client-supplied "owner" field to the resource, since an unauthenticated identity string reads as access control without being it (the `AnnotationStore` author field is scoped as a coordination namespace precisely to avoid that) |
