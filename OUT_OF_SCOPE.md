# Out of scope — the deferred-work ledger

One line per item a PR deliberately deferred without a roadmap ticket. The contract:

- A PR that lists something under "Out of scope" adds it here in the same PR.
- An entry names the source PR (plain text, no backlink) and the natural pickup trigger.
- Remove the entry when the item lands or gets promoted to a roadmap ticket. Items that
  need evidence tracking, affect correctness, or have no adjacent future work get a
  ticket instead of a line here.
- **An entry earns its place only if nobody could pick it up deliberately.** That is the test.
  If someone could schedule it, it is a ticket. If it can never be picked up at all because
  the question is settled, it is a decision and belongs in `DECISIONS.md`. What is left is the
  narrow middle: work that becomes obvious and cheap the moment an unrelated trigger fires, and
  is not worth anyone's attention before then — "rename this when you are next in this file",
  "add the fixture when a design that needs it turns up".
- **Sweep it periodically.** This file is only ever appended to in the course of normal work,
  so it drifts in two directions on its own: entries stay after the thing they asked for is
  built (a stale line reads as current, which is worse than a long list), and one piece of
  work gets written down many times as several PRs each defer their own slice of it. Both
  need a read-through to see; neither is visible one entry at a time. The 2026-08 sweep took
  108 entries to 15, promoting eighteen clusters and singletons to tickets, moving three
  settled questions to `DECISIONS.md`, and deleting what had already landed.
- **Nothing private.** This file is world-readable. A pickup trigger that cannot be stated
  without naming a customer, a private path, or a corpus location belongs in the private
  workspace, not here. Sanitize when writing the line, not later; deleting it afterwards does
  not remove it from history.

| Item | From | Pick up when |
|------|------|--------------|
| Details-panel summary names the sheet the design OPENED on and never updates as you navigate sheets, so it reads stale ("Hier root · faithful" while amp1 is shown). Pre-existing; the WS9-049 tab strip makes it visible by putting the active sheet name right beside it | WS9-049 PR1 | someone re-does the Details panel, or a user reports the mismatch; the fix is to push the summary per sheet rather than per design load |
| `Validate` RPC serving `webapi.ValidateReport` to the viewer | PR 83 | WS9 wants a validation panel; the message shape is ready |
| `.edn` content sniffing, per-format validate knobs, unresolved-vs-empty cause split | PR 83 | ticketed as WS6-008 (listed here only until it lands) |
| Move `svg/` under `internal/` | PR 69 | next PR touching `svg/` |
| Rename `edif.ReadSchematic` to `ReadSchematicGeometry` (match the kicad naming) | PR 69 | next PR touching the edif reader surface |
| Tests for the remaining render-only islands (controlbar, expectationspanel) | PR 67 | next web PR; scaffolding exists; findingspanel landed in PR 84 |
| Corpus-scale parse benchmarks | PR 67 | a perf question actually arises; fixture benchmarks are the tripwire |
| `stats --format json` (canonical wire shape question; `diff --format json` landed with WS9-004) | PR 68 | someone needs machine-readable stats; decide the proto first |
| WebGL rendering for the visual diff (resolve sideSpecs locally over packed keys) | WS9-005 PR | WS9-007 overlay work, or when WebGL fidelity matches SVG |
| Conformance fixtures for ipc2581 / xschem / geda source formats | PR 65 | those readers gain geometry-bearing fixtures worth asserting rules over |
| WS3-072: the remaining per-rule `isPowerRailName`/`isGroundName`/`isFeedbackName` call sites (reach.go, pinrole.go, the rule Evals) still call the name lexicon; only the net.ground/feedback/IsPowerRail projectors read the stamped `net.role`. Mechanical, behavior-preserving (stamped = name-derived in PR1) | WS3-072 PR1 | migrate a call site to `netHasRole` when it is next touched; the FFIs (`rail_name`/`ground_name`/`feedback_name`) operate on a bare name literal with no net binding, so they keep the name lexicon by design |
| WS9-035 params panel reads via a bespoke `GetComponentParams` RPC (returns the structured `PartSpec`) rather than the datalog query surface (`component.mpn` ⋈ the WS10-010 `param(...)` relations). Considered the query route; kept the RPC because the panel's tree needs the full nested spec — conditions, provenance/citation, limit-kind, min/typ/max — and the flat `param(...)` relations do not carry all of that yet | WS9-035 PR 313 | WS3-082 enriches `param(...)` with min/limit_kind (and ideally conditions/provenance); then the panel can migrate to a datalog query over the existing QueryService and drop the RPC + adapter. Separately: point serve's provider at a real service-backed `param.ParamProvider` (the WS10-010 seam) instead of only `--params <dir>` |
| WS3-107: the services still hold a BUILT `*check.Catalog` rather than the sources that composed it, so `Overlay.Catalog` extends a catalog it cannot re-derive. `Catalog.With` makes the drop impossible at the one site that had it, but the shape still permits a future caller to rebuild and lose the base | WS3-107 PR | the ticket's preferred fix: thread a "catalog inputs" value (`[]check.RuleSource`) to every surface so a catalog is always composed from its inputs and this class of drop is structurally impossible. It is a signature change across ~20 call sites, mostly tests, which is why it was kept out of a P1 bug fix |
| WS3-092: `intent.DocRules` stamps every intent doc page `severity: warning`, so the docsite catalog understates the three intent rules whose runtime severity is `error` (`rail-current-capacity` from WS3-095, `sequence-<slug>` here, `load-switch-trip-below-budget` from WS3-085). The runtime findings carry the right severity; only the generated reference page is wrong | WS3-092 PR | the doc-rule projection is touched for any reason; carry the per-kind severity in a map beside `docSummaries` rather than hardcoding one value |
| The TXB0104 param fixture omits the pin table's "Thermal pad" row, which has a name and a description but no number in any package. That zero-numbers case is legal in the contract and exercised by nothing | PR 200 | someone is next in `datasheet/param/testdata/txb0104.textproto`, or an extractor meets a pin-table row with no number and needs to know the shape is supported |
| `pin-exceeds-abs-max` and `pin-out-of-recommended` quantify over SUPPLY pins only, matching the two alias-path rules they supersede. The contract can express a limit on any terminal, so a datasheet stating one on a logic input or an output is bound and queryable but unchecked | PR 205 | a rule wants a non-supply pin's own limit; the resolve-and-compare loop (`eachPinLimit`) already handles any pin, so it is the `SupplyInputPin` filter that moves, plus a new false-positive surface to think about |
| A parameter bound to many terminals renders every name inline, in both the viewer's parts panel and the workbench's binding chips. Eight is the realistic worst case (a port-wide rating on the TXB0104) and it is wide but readable, so no truncation shipped. `partspanel.test.tsx` characterises the untruncated output so the decision cannot reverse silently | PR 207, PR 208 | a real part makes a row unreadable, or the panel gets narrower; the test that asserts the full string is the one to change |
| `review --min-answered` takes a COUNT, not a ratio. A count is what a team reads straight off the report, and it is the right shape while a checklist is stable; it silently loosens as the checklist grows, since adding an unanswered item lowers the ratio and leaves the count where it was | issue 199 PR | someone's checklist grows enough that they notice, or a second consumer asks. `N%` is a ~5-line parse in `parseReviewGate` over the same `Tally.Answered()` |
| The viewer's parts panel renders a part's parameters and their pin bindings but not its `PinRelation`s, so a relation seeded into a corpus is authorable and queryable-by-API yet invisible to someone reading the design. The workbench that authors them does show them | PR 234 | someone is next in `partspanel.tsx`, or a user asks why a relation they transcribed does not appear beside the part; `param.PinRelations` already returns them from either end and `fmtRelation` in `bank.ts` already renders one as a sentence |
| A pin table's package column yields `Package{id, name, mpn_suffix}` all from the printed code, so the body name is the code (`PW`) rather than the descriptive name (`PW (TSSOP-14)`) the hand-encoded fixture carries. The descriptive name lives in the ordering-information table, which no recipe classifies | PR 240 | someone adds an ordering-information rule; the join is the same `mpn_suffix` the pin table already emits |
| The two pin-tracking rules have no fixture under `cmd/agni/testdata/conformance/`, so nothing exercises them through the CLI. The unit tests cover the logic and the PR's before/after was captured from a real run against a scratch fixture, but neither is committed. The sibling per-pin rules are in the same position | issue 192 PR | someone adds a conformance fixture for any datasheet rule, or the scratch design in that PR is wanted again; it is a ~15-line `.edn` plus one synthetic spec carrying a relation |
| `review.Run` calls `check.Run` once per checklist item, so items sharing a binding sweep the same rule repeatedly (step 2 of issue 198). Measured competitive with a single sweep — a 44-item auto-manifest ran in 27ms against `check`'s 46ms on the tutorial fixture — so the performance argument that motivated it did not survive, and it is now a tidiness change with no driver | issue 198 assessment | someone is next inside `review.Run` for another reason; grouping by binding before sweeping is local and obvious once you are there |
| `param.MarkVerifiedIn` (Go) still has no caller. The workbench writes verifications through its own TS builder (`bank.handVerification`), so there are now TWO places that construct a `Verification` and they must agree on the rules (refuse without a recorded revision, refuse anonymous, snapshot the title, raise confidence). They agree today and nothing enforces it | issue 261 PR 1 | a Go-side portal or a bulk pass (issue 262) needs to verify; either make the TS path call a service method that delegates to `MarkVerifiedIn`, or add a shared conformance case. Note the Go helper additionally raises `prov.confidence` to 1.0 while the TS path gets that for free from `newParameter`, which is the kind of drift to watch |
| A hand-transcribed PIN declaration is as much a human claim as a parameter, but `Verification` sits only on `Parameter`, so pins cannot record one. Consistent with the check path, where a pin's citation has always carried no verification, but it means a pin function transcribed off a page never expires when the document is reissued | issue 261 PR 1 | a pin-rating rule starts resting on transcribed pin data, or someone adds `Verification` to `Pin`; the plumbing mirrors the parameter path exactly (`DatasheetCitationOfProv` is the seam that currently returns empty) |
| A pin number renders at the SYMBOL's designator origin even when the placing instance overrides it, because a per-instance override needs per-placement pin data and `SymbolDef` is shared across placements by construction. On one export this puts the number a row above where the tool prints it; everything else about that component matches, x-positions included | PR 303 | someone adds per-placement pin overrides for another reason (a per-instance pin swap, bank-specific numbering), or a reader arrives whose pin numbers are genuinely per-instance rather than per-symbol |
| The title block is the last text sized from a guessed factor rescued by a pixel ceiling (`math.Max(6, math.Min((yBot-yTop)*0.5, 13))` in `render/svg.go`), the same shape as the zone-ruler bug that was fixed by measuring. It was left because the title block is furniture synthesized from the page size rather than text the design states, so there is no source height to read and no clean reference to calibrate against | PR 302 | someone renders a title block against a reference print and can measure the factor, or a sheet size turns up where 13px is visibly wrong |
| `internal/refdes` is the single definition of what a placeholder designator is, and it is `internal/`, so an out-of-module reader registering through `readers/formats` (the C18 overlay seam) cannot reach it and would have to write the predicate again. That is how the in-tree copy this PR removed came about. No out-of-module reader exists yet, so nothing is broken today | PR for issue 311 | an overlay reader needs to decide whether a placement is annotated; the move is mechanical (a re-export from a public package, or promoting `refdes` beside `readers/formats`), and the package comment already argues why the predicate has to be shared rather than per-reader |
| The prune walk is uncached: the client caches a listing but the server caches nothing, so re-opening a folder pays the same walk again, and the mount-level walk repeats on every page load. Bounded (depth 8, 2000 directories per request) and unmeasured on a large mount, which is why no cache shipped. Two obvious moves when it matters: memoize the per-directory answer keyed by (mount, path) against the directory's mtime, and have the walk report how deep it got so a parent re-listing does not restart from the top | PR for the empty-directory fix | expanding a folder feels slow on a real mount, or a mount lands with a vendored/source tree under it; both moves live entirely inside `hasOpenableFile` and its callers |
| Docs search has no "no exact match" guard, so a query matching nothing degrades to a loose match rather than returning zero. `agniRun` (a build-time directive that never reaches rendered HTML) matches all 170 pages, and a typo returns a handful of confidently wrong pages. Pagefind's `termSimilarity` only reorders and cannot filter it, and the result score does not separate a valid rare term from a garbage one, so any guard has to be client-side | docsite search PR | someone reports that a nonsense query returns the whole site, or the search box gets revisited for another reason; the cheap version is to re-run the query quoted and show a "no exact matches" note when the quoted form returns zero |
| A results document cannot carry a considered set. `CheckResults` has no verdicts field, so `agni check --results-out` writes findings only and `agni results` can never replay a pass. The round-trip test enforces that the two formats agree, which is why the field was NOT added here: a persisted contract should gain a field when something wants to persist it, not so a print statement has an envelope to marshal | verdicts-on-the-wire PR | someone wants a stored run to answer "what was checked" rather than "what failed", most likely the first time a considered set has to be compared across two revisions |
| A failing verdict carries two prose fields that nearly restate each other: `Witness.Statement` and `Finding.Message`. THE TRIGGER HAS FIRED — around 26 rules have converted and the sample is no longer thin, and the answer is that NEITHER derives from the other. On the datasheet rules `Message` is strictly richer (it carries the citation inline); on `i2c-pull-up` and the protection rules `Statement` is more specific (it names the clamp, the fuse, the complement); only on the structural rules do they genuinely duplicate. `Witness` also exists on a Pass where no `Finding` does. `core/report` now prints both, which is the second consumer that has to choose | verdicts-on-the-wire PR; evidence gathered issue 391 PRs 400/404 | this is now schedulable rather than blocked, so it wants a ticket rather than a line here. The shape of the answer is probably a rule stating one of them and the other being generated where they duplicate, decided per rule family rather than globally |
| `i2c-pull-up` still declares `select` in `Primitives`, which its Go Eval no longer composes now that the body loops over `m.Nets()` directly. Nothing fails, because `TestSpecMetadata` validates that field against the SPEC twin and the twin is unchanged. The drift is between the field's stated meaning ("query primitives Eval composes") and the Go body it names | proof-on-pass PR (three rules) | someone is next in `rule_i2c_pull_up.go`, or the next twinned rule converts to verdicts and raises the same question. Either restate the field as the twin's composition, which is what it is already checked against, or keep the Go body composing `Select` |
| A spec pass can only state a value when the predicate MEASURED one, and after issue 412 the residue is narrow: an ARGUMENT-LESS `Call`. A call carrying arguments now names the values it was given (`net.names is "GND", which ground_name accepts`), because the interpreter can read a Term even where the FFI hands back a bare bool. `intentionally_unconnected`, `tvs_reach`, `power_pin_reach`, `ic_esd_rated` and `led_reversed` take the whole scope and return a verdict, so there is nothing to read; they now say so plainly rather than printing the rule's syntax, and TestSpecPassWitnessAdmitsAnArgumentLessCallStatesNoValue pins it | proof-on-pass value PR; narrowed by issue 412 PR | a SpecFunc gains a way to hand back what it observed alongside its verdict, which `cap-voltage` needs anyway; solving it once covers both |
| The viewer's `findingsFor` matches a click against a finding's SUBJECT only, ignoring `f.context`, so clicking the second entity of a pair finding still finds nothing. Its own comment says the fix "needs a structured context field on Finding rather than a cleverer filter", and that field has existed since agni issue 349; the cross-tool comparison in `core/results` made the equivalent change and it was three lines. Left out because it changes CLICK SEMANTICS — clicking R1 would surface findings that merely mention R1 — which is a product decision rather than a bug fix | N-ary verdict subjects PR | someone is next in `web/src/findings.ts`, or a user asks why clicking one end of a clearance violation finds it and the other end does not |
| The glossary sweep is done for every repeat a GREP can find, and the tail cannot be found that way. Bolding catches a term introduced as a definition and misses an unbolded gloss, so the survey that produced `pull-up` and `pull-down` as the only remaining `learn/` repeats is a floor rather than a complete list. Twenty terms are tagged at 67 sites across guide/, tutorials/, learn/, architecture/ and reference/, and every EE level from EE1 to EE7 now has at least one entry. The second wave picked its terms by FREQUENCY (each appears in ten or more files) rather than by reading, which is a different instrument from the bolding grep and found eleven the grep had missed. Two candidates were rejected on the same evidence: `open-drain` and `series resistor` score well on raw hits but have no mention outside the course, so tagging either would have meant forcing a reference to satisfy `terms_test.go` rather than because a page wanted one | glossary sweep PR, narrowing the terms PR row | someone reads a `learn/` chapter end to end for another reason and notices a gloss repeated from an earlier chapter. Adding it is one term page plus one index line, and the convention (tag a later mention, never a first teaching, once per page) is in `docsite/README.md` |
| A term declares an EE level in its frontmatter (`level: EE2`) and nothing reads it. `learn/levels.md` already maps every chapter section to a level and `learn_levels_test.go` enforces that map, so a per-level glossary view or an "explain this at EE3" filter has both halves of its data and no consumer | terms PR | someone wants the glossary grouped or filtered by level, or the levels vocabulary grows a second consumer; `TermIDs` already returns the parsed terms and the index page is the obvious place it would render |
| `agni.yaml` resolution is first-hit-wins rather than merging up the walk, so a repo-level file and a personal one are mutually exclusive: a lane that carried its own fixture mounts would shut out `~/.config/agni/agni.yaml` entirely, and the workaround is to hand the whole table to one of them (`make serve MOUNTS=`). The reason for first-hit-wins is recorded in `DECISIONS.md` and in `envconfig.go`, that a merged table would make the effective mounts depend on which directory a command ran from. Nothing merges today, so nothing is broken | Makefile-is-the-interface PR | someone wants a checked-in `agni.yaml` shipping the fixture mounts AND a personal file for their own designs, which is the first case where the two must compose. A merge would have to say what happens per key (mounts accumulate, a duplicate name resolves to the nearest) and the `DECISIONS.md` entry wants rewriting rather than contradicting |
| `architecture/web-app.md` is 6,741 words on-page after this pass, roughly the next two pages combined, and it is three subjects in one file: the presenter contract, the Connect service reference, and an accumulated list of web-tier traps. The readability pass could shorten it and could not fix its shape, because splitting a page is a nav change (four edits plus `nav_test.go`) rather than a prose edit | architecture readability PR | someone adds a fourth subject to it, or a reader cannot find the service table. The trap list is the cleanest cut, since nothing above it refers into it |
| Six diagrams are wider than the roughly 4:1 ceiling `docsite/README.md` sets, so the site scales them down, and they split across the two mechanisms. FIVE are mermaid blocks, found by rendering all 29 on the site with `mmdc` and reading the ratios: `reference/terms/termination` at 9.22:1, `reference/terms/transceiver` at 7.76:1, `reference/terms/differential-pair` at 4.93:1, `reference/terms/port-protection` at 4.72:1, and `build/the-gate` block 2 at 4.48:1. The first two are the ones that actually read badly, at 784x85 and 729x94. ONE is a hand-authored figure, `figures/derating.svg` at 660x156, which is 4.23:1 and marginal. All six predate the figure work | architecture readability PR, corrected by the checkpoint after a full mermaid sweep | someone edits one of those pages. A wide `LR` fan-out usually becomes a legible `TB` stack, which is the fix the README already names, and each rework needs its ratio re-read afterwards |
| A guide command fence is hand-written and nothing checks it. 37 of them across nine `guide/` pages, and four were stale: `getting-started.md` reported 10 findings where the fixture now produces 11 and named 29 rules where it runs 78, and both its fences plus both of `interface-profiles.md`'s predated the coverage summary line the engine now prints. Found by grepping for a finding count with no coverage line after it, not by any test. `hack/tutorial_runs_check.sh` already walks `docsite/content/**/runs/`, so converting a guide page to generated `agniRun` captures needs a `runs/` directory and one spec per fence and no change to the harness | healthcheck and stale-fence PR | someone edits a guide fence, or a release changes `check`'s summary line again. Convert that page rather than re-typing the output, since the fences that went stale are exactly the ones nobody re-ran |
| `naming-conventions.md`'s "no findings (1 rule(s) run)" fence cannot be reproduced or generated, because the command it shows names `design.edn` and `example.yaml`, which are illustrative rather than files in the repo. It is the one fence left without the coverage line every other guide fence now carries | healthcheck and stale-fence PR | that page gets a real fixture to run against, which is also what would let it become a capture. The tutorial project's `conventions.yaml` and `designs/gateway/gateway.edn` are the obvious pair |
| A canvas note strip's TEMPLATE HOLE is unchecked. `undrawnStrip` and `staleLinkStrip` both take `document.getElementById(...)` and no-op on null, so deleting `<div id="undrawn-note">` or `<div id="stale-link-note">` from `ViewerPage.html` leaves every test green and the strip silently dead. Verified by removing the hole: all 12 `composition.test.ts` tests still pass. The `ViewSink` channel IS covered (the view-port test plus the compiler); only the DOM anchor is not, because the hole test walks `data-component` islands and a note strip is a plain div | PR 490 (issue 392 acceptance 3) | someone adds a third note strip, or `composition.test.ts` is opened for another reason. The cheap version is to assert the ids the composition root looks up are present, which is a list the root already has |
| `osLoader.DesignHash` hashes the path it is given while `localLoader.DesignHash` resolves the design descriptor first, so the served and CLI sides of the same method mean different things. Not reachable as a defect today, because every link the CLI mints now names a real entry file and the server is only ever asked about that | PR for issue 489 | `GetDesign` is asked to accept a design FOLDER (the wider of the two fixes issue 489 wrote up), or a second server-side caller needs a hash of something a descriptor redirects. Widening the served loader is a change to `service.Loader`'s contract and wants its own ticket |
