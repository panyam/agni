// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: agni/v1/webapi/design.proto

package webapiconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	webapi "github.com/panyam/agni/gen/go/agni/v1/webapi"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// DesignServiceName is the fully-qualified name of the DesignService service.
	DesignServiceName = "agni.v1.webapi.DesignService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// DesignServiceGetDesignProcedure is the fully-qualified name of the DesignService's GetDesign RPC.
	DesignServiceGetDesignProcedure = "/agni.v1.webapi.DesignService/GetDesign"
	// DesignServiceGetSheetProcedure is the fully-qualified name of the DesignService's GetSheet RPC.
	DesignServiceGetSheetProcedure = "/agni.v1.webapi.DesignService/GetSheet"
	// DesignServiceHighlightSheetProcedure is the fully-qualified name of the DesignService's
	// HighlightSheet RPC.
	DesignServiceHighlightSheetProcedure = "/agni.v1.webapi.DesignService/HighlightSheet"
	// DesignServiceGetLayoutReportProcedure is the fully-qualified name of the DesignService's
	// GetLayoutReport RPC.
	DesignServiceGetLayoutReportProcedure = "/agni.v1.webapi.DesignService/GetLayoutReport"
)

// DesignServiceClient is a client for the agni.v1.webapi.DesignService service.
type DesignServiceClient interface {
	// GetDesign loads a file and returns its summary plus the list of drawable sheets (the
	// sheet navigator's source). It resolves the render mode per file (faithful for geometry,
	// else an auto-layout).
	GetDesign(context.Context, *connect.Request[webapi.GetDesignRequest]) (*connect.Response[webapi.GetDesignResponse], error)
	// GetSheet returns one rendered sheet. The `format` selects the renderer: PACKED (the
	// tier-2 geometry for the WebGL viewer, C8), SVG (the render.SheetSVG verification
	// backend), or NATIVE (the format's own tool, a golden reference; not yet implemented).
	// `sheet` selects by id, name, or 0-based index (as the CLI does).
	GetSheet(context.Context, *connect.Request[webapi.GetSheetRequest]) (*connect.Response[webapi.GetSheetResponse], error)
	// HighlightSheet resolves highlight specs ("these components, nets, and pins, in this
	// color/alpha") against one rendered sheet and returns a drawable overlay layer, decoupled
	// from the base render: PACKED yields primitive-index groups that join the GetSheet
	// PackedSheet by index (the WebGL viewer redraws just those primitives), SVG yields a
	// transparent overlay document with the same viewBox as the GetSheet SVG (the client
	// stacks or composites it above the sheet). NATIVE has no overlay and is rejected. The
	// request's sheet/layout/symbols must match the GetSheet call that produced the base
	// render, so the two layers describe the same geometry.
	HighlightSheet(context.Context, *connect.Request[webapi.HighlightSheetRequest]) (*connect.Response[webapi.HighlightSheetResponse], error)
	// GetLayoutReport explains how an auto-layout maps each component to a drawn node (device class,
	// and whether it drew a glyph, the generic box, the design's own symbol, or an unresolved
	// fallback). It uses the same symbol source as the render, so it describes what the viewer
	// shows and points at --symbol-path when provided symbols do not resolve.
	GetLayoutReport(context.Context, *connect.Request[webapi.GetLayoutReportRequest]) (*connect.Response[webapi.GetLayoutReportResponse], error)
}

// NewDesignServiceClient constructs a client for the agni.v1.webapi.DesignService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewDesignServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) DesignServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	designServiceMethods := webapi.File_agni_v1_webapi_design_proto.Services().ByName("DesignService").Methods()
	return &designServiceClient{
		getDesign: connect.NewClient[webapi.GetDesignRequest, webapi.GetDesignResponse](
			httpClient,
			baseURL+DesignServiceGetDesignProcedure,
			connect.WithSchema(designServiceMethods.ByName("GetDesign")),
			connect.WithClientOptions(opts...),
		),
		getSheet: connect.NewClient[webapi.GetSheetRequest, webapi.GetSheetResponse](
			httpClient,
			baseURL+DesignServiceGetSheetProcedure,
			connect.WithSchema(designServiceMethods.ByName("GetSheet")),
			connect.WithClientOptions(opts...),
		),
		highlightSheet: connect.NewClient[webapi.HighlightSheetRequest, webapi.HighlightSheetResponse](
			httpClient,
			baseURL+DesignServiceHighlightSheetProcedure,
			connect.WithSchema(designServiceMethods.ByName("HighlightSheet")),
			connect.WithClientOptions(opts...),
		),
		getLayoutReport: connect.NewClient[webapi.GetLayoutReportRequest, webapi.GetLayoutReportResponse](
			httpClient,
			baseURL+DesignServiceGetLayoutReportProcedure,
			connect.WithSchema(designServiceMethods.ByName("GetLayoutReport")),
			connect.WithClientOptions(opts...),
		),
	}
}

// designServiceClient implements DesignServiceClient.
type designServiceClient struct {
	getDesign       *connect.Client[webapi.GetDesignRequest, webapi.GetDesignResponse]
	getSheet        *connect.Client[webapi.GetSheetRequest, webapi.GetSheetResponse]
	highlightSheet  *connect.Client[webapi.HighlightSheetRequest, webapi.HighlightSheetResponse]
	getLayoutReport *connect.Client[webapi.GetLayoutReportRequest, webapi.GetLayoutReportResponse]
}

// GetDesign calls agni.v1.webapi.DesignService.GetDesign.
func (c *designServiceClient) GetDesign(ctx context.Context, req *connect.Request[webapi.GetDesignRequest]) (*connect.Response[webapi.GetDesignResponse], error) {
	return c.getDesign.CallUnary(ctx, req)
}

// GetSheet calls agni.v1.webapi.DesignService.GetSheet.
func (c *designServiceClient) GetSheet(ctx context.Context, req *connect.Request[webapi.GetSheetRequest]) (*connect.Response[webapi.GetSheetResponse], error) {
	return c.getSheet.CallUnary(ctx, req)
}

// HighlightSheet calls agni.v1.webapi.DesignService.HighlightSheet.
func (c *designServiceClient) HighlightSheet(ctx context.Context, req *connect.Request[webapi.HighlightSheetRequest]) (*connect.Response[webapi.HighlightSheetResponse], error) {
	return c.highlightSheet.CallUnary(ctx, req)
}

// GetLayoutReport calls agni.v1.webapi.DesignService.GetLayoutReport.
func (c *designServiceClient) GetLayoutReport(ctx context.Context, req *connect.Request[webapi.GetLayoutReportRequest]) (*connect.Response[webapi.GetLayoutReportResponse], error) {
	return c.getLayoutReport.CallUnary(ctx, req)
}

// DesignServiceHandler is an implementation of the agni.v1.webapi.DesignService service.
type DesignServiceHandler interface {
	// GetDesign loads a file and returns its summary plus the list of drawable sheets (the
	// sheet navigator's source). It resolves the render mode per file (faithful for geometry,
	// else an auto-layout).
	GetDesign(context.Context, *connect.Request[webapi.GetDesignRequest]) (*connect.Response[webapi.GetDesignResponse], error)
	// GetSheet returns one rendered sheet. The `format` selects the renderer: PACKED (the
	// tier-2 geometry for the WebGL viewer, C8), SVG (the render.SheetSVG verification
	// backend), or NATIVE (the format's own tool, a golden reference; not yet implemented).
	// `sheet` selects by id, name, or 0-based index (as the CLI does).
	GetSheet(context.Context, *connect.Request[webapi.GetSheetRequest]) (*connect.Response[webapi.GetSheetResponse], error)
	// HighlightSheet resolves highlight specs ("these components, nets, and pins, in this
	// color/alpha") against one rendered sheet and returns a drawable overlay layer, decoupled
	// from the base render: PACKED yields primitive-index groups that join the GetSheet
	// PackedSheet by index (the WebGL viewer redraws just those primitives), SVG yields a
	// transparent overlay document with the same viewBox as the GetSheet SVG (the client
	// stacks or composites it above the sheet). NATIVE has no overlay and is rejected. The
	// request's sheet/layout/symbols must match the GetSheet call that produced the base
	// render, so the two layers describe the same geometry.
	HighlightSheet(context.Context, *connect.Request[webapi.HighlightSheetRequest]) (*connect.Response[webapi.HighlightSheetResponse], error)
	// GetLayoutReport explains how an auto-layout maps each component to a drawn node (device class,
	// and whether it drew a glyph, the generic box, the design's own symbol, or an unresolved
	// fallback). It uses the same symbol source as the render, so it describes what the viewer
	// shows and points at --symbol-path when provided symbols do not resolve.
	GetLayoutReport(context.Context, *connect.Request[webapi.GetLayoutReportRequest]) (*connect.Response[webapi.GetLayoutReportResponse], error)
}

// NewDesignServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewDesignServiceHandler(svc DesignServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	designServiceMethods := webapi.File_agni_v1_webapi_design_proto.Services().ByName("DesignService").Methods()
	designServiceGetDesignHandler := connect.NewUnaryHandler(
		DesignServiceGetDesignProcedure,
		svc.GetDesign,
		connect.WithSchema(designServiceMethods.ByName("GetDesign")),
		connect.WithHandlerOptions(opts...),
	)
	designServiceGetSheetHandler := connect.NewUnaryHandler(
		DesignServiceGetSheetProcedure,
		svc.GetSheet,
		connect.WithSchema(designServiceMethods.ByName("GetSheet")),
		connect.WithHandlerOptions(opts...),
	)
	designServiceHighlightSheetHandler := connect.NewUnaryHandler(
		DesignServiceHighlightSheetProcedure,
		svc.HighlightSheet,
		connect.WithSchema(designServiceMethods.ByName("HighlightSheet")),
		connect.WithHandlerOptions(opts...),
	)
	designServiceGetLayoutReportHandler := connect.NewUnaryHandler(
		DesignServiceGetLayoutReportProcedure,
		svc.GetLayoutReport,
		connect.WithSchema(designServiceMethods.ByName("GetLayoutReport")),
		connect.WithHandlerOptions(opts...),
	)
	return "/agni.v1.webapi.DesignService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case DesignServiceGetDesignProcedure:
			designServiceGetDesignHandler.ServeHTTP(w, r)
		case DesignServiceGetSheetProcedure:
			designServiceGetSheetHandler.ServeHTTP(w, r)
		case DesignServiceHighlightSheetProcedure:
			designServiceHighlightSheetHandler.ServeHTTP(w, r)
		case DesignServiceGetLayoutReportProcedure:
			designServiceGetLayoutReportHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedDesignServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedDesignServiceHandler struct{}

func (UnimplementedDesignServiceHandler) GetDesign(context.Context, *connect.Request[webapi.GetDesignRequest]) (*connect.Response[webapi.GetDesignResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("agni.v1.webapi.DesignService.GetDesign is not implemented"))
}

func (UnimplementedDesignServiceHandler) GetSheet(context.Context, *connect.Request[webapi.GetSheetRequest]) (*connect.Response[webapi.GetSheetResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("agni.v1.webapi.DesignService.GetSheet is not implemented"))
}

func (UnimplementedDesignServiceHandler) HighlightSheet(context.Context, *connect.Request[webapi.HighlightSheetRequest]) (*connect.Response[webapi.HighlightSheetResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("agni.v1.webapi.DesignService.HighlightSheet is not implemented"))
}

func (UnimplementedDesignServiceHandler) GetLayoutReport(context.Context, *connect.Request[webapi.GetLayoutReportRequest]) (*connect.Response[webapi.GetLayoutReportResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("agni.v1.webapi.DesignService.GetLayoutReport is not implemented"))
}
